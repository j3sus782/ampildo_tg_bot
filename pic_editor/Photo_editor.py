#скрипт для создания демотиватора из отправленных фото
from PIL import Image, ImageFont, ImageDraw

"""""
def photo_edit(img_text):

    with Image.open('template.jpeg') as template:
    with Image.open('saved.jpeg') as saved:

    width = 610
    height = 569
    font_width = 60
    font = ImageFont.load('font.ttf')

    resiz_saved = saved.resize(width, height)

    draw = ImageDraw.Draw(template)

    strip_width, strip_height = 700, 1300

"""""